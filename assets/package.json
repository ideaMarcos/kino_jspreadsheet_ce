{
  "private": true,
  "scripts": {
    "build": "esbuild ./kino/main.js --bundle --minify --outfile=../lib/assets/kino_build/main.js --format=esm && esbuild ./smart_cell/main.js --bundle --minify --outfile=../lib/assets/smart_cell_build/main.js --format=esm",
    "dev": "pnpm run build -- --watch"
  },
  "dependencies": {
    "jspreadsheet-ce": "^5.0.4",
    "jsuites": "^5.13.5"
  },
  "devDependencies": {
    "esbuild": "^0.27.4"
  }
}